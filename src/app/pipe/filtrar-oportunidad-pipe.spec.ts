import { FiltrarOportunidadPipe } from './filtrar-oportunidad-pipe';

describe('FiltrarOportunidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarOportunidadPipe();
    expect(pipe).toBeTruthy();
  });
});
